class Main {
	This is a comment*)
      bar : B;
    baz: A;

    main(): B {
    {
      bar <- (new B).init();
      baz <- (new A).init(111, "TAC generation");
      bar.createFoo();
    }
  };
};
