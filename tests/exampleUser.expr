class Main {

  a: Int;
  b: Int;

  main(): SELF_TYPE { self };

  fun(x: Int, y: Int): SELF_TYPE {
    {
      b <- y;
      while b + 0 loop 
        b <- b - 1
      pool;
      if b *  10 then 
        b <- b + 1
      else b <- 2
      fi;
      self;
    }
  };
};


